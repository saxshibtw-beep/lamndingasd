/* Copy this file to notify-config.js and fill in (optional).
   Used for “someone tapped Download” messages. Same bot or a separate bot is fine. */
Object.assign(window.LUXY_NOTIFY || {}, {
  botToken: '',
  chatId: ''
});
